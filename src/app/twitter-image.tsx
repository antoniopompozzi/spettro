/**
 * The same card as the Open Graph one. Twitter reads its own tags, and a
 * second drawing would only be a second thing to keep in step with the first.
 */
export { default, alt, size, contentType } from './opengraph-image';
